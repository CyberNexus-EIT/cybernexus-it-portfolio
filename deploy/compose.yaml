services:
  cybernexus-it-portfolio:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: cybernexus-it-portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
